# AGENTS.md — Журнал изменений и руководство для агентов

Этот документ обязателен для ведения всеми агентами, работающими над проектом AdventGame.
При начале работы агент **обязан** ознакомиться с данным файлом для понимания архитектуры, истории исправлений и корневых причин (root causes) изменений. Все новые значимые изменения должны в подробностях фиксироваться здесь.

---

## Архитектура проекта

AdventGame — шестидневная интерактивная игра-адвент для Сонечки с малышом Грутиком и Веномом.
Каждый день открывается новое испытание, за победу в котором игрок получает возможность прокрутить Колесо Фортуны и забрать скрытый подарок из пула `gifts.js`.

### Структура файлов
- **`index.html`**: Главная разметка, секции экранов (`screenGame`, `screenWheel`, `screenWait`, `screenFinal`, `screenStory`), модальное окно подарков.
- **`style.css`**: Оформление, анимации, стили мини-игр, палитра цветов блоков, эффекты появления Венома и праздничного конфетти.
- **`app.js`**: Логика прогресса, переход по дням, сторилайн (массивы `INTRO`, `GREET`, `POSTWIN`, `AFTERGIFT`, `FINAL_SCENE`), колесо фортуны, сохранение в `localStorage`.
- **`games.js`**: 6 мини-игр испытаний:
  1. `memory` (День 1) — «Наши моменты»: нахождение парных фотографий (сетка 5×5 с центральным фото).
  2. `blockblast` (День 2) — «Block Blast: корни»: размещение фигур, очистка рядов/колонок, бесконечный режим на очки.
  3. `echo` (День 3) — «Эхо сигнала»: повторение последовательностей сигналов.
  4. `photoPuzzle` (День 4) — «Собери нашу фотографию»: фигурный фотопазл 4×4 из 16 кусочков.
  5. `circuit` (День 5) — «Живая схема»: вращение узлов для восстановления цепи питания колеса.
  6. `finale` (День 6) — «Протокол SONECHKA»: трёхфазный финал (код $\to$ баланс $\to$ импульсы).
- **`grutik.js`**: Рендеринг многослойного Грутика на canvas, расчёт анимаций, стадии трансформации (0..8) и реплики спутника (`GRUTIK_LINES`).
- **`gifts.js`**: Конфигурация игры (`SITE_CONFIG`) и массив подарков (`GIFT_POOL`).
- **`tests/game_logic.test.js`**: Набор модульных тестов логики стадий, сюжета, кнопок и механик Block Blast.

---

## История изменений

### 2026-09-13: Доработка сюжета, стадий Грутика, колеса фортуны и Block Blast

#### 1. Реплики и сюжетная согласованность
- **Реплика в `grutik.js:4` (`GRUTIK_LINES[2]`)**:
  - *Проблема*: Строка содержала `"Маршрут уже построен. Почти."`, оставшуюся от старой игры `trace`. День 2 теперь — Block Blast.
  - *Решение (Root Cause)*: Заменена на тематическую реплику про блоки корней: `"Блоки готовы. Расчистим место для корней!"`.
- **Приветствие Дня 1 (`GREET[0]` в `app.js`)**:
  - *Проблема*: Диалог описывал устаревшую игру «импульс» (светящееся сердце, ритм, рассыпающийся импульс), хотя день 1 — поиск парных фотокарточек.
  - *Решение (Root Cause)*: Реплики переписаны под поиск воспоминаний: карточки с моментами рассыпались возле горшочка, Грутик открывает одну и забывает пару, Сонечка помогает собрать воспоминания.
- **Приветствие Дня 4 (`GREET[3]` в `app.js`)**:
  - *Проблема*: После появления Венома не было связи с фотопазлом, а в `AFTERGIFT[3]` Грутик внезапно смотрел на собранное фото.
  - *Решение (Root Cause)*: В момент появления Венома общая фотография разлетается на кусочки пазла. Веном и Грутик спорят, кто соберёт её первым («МЫ СОБЕРЁМ ЕЁ ПЕРВЫМИ»), Сонечке предлагается восстановить фото.
- **Приветствие Дня 5 (`GREET[4]` в `app.js`)**:
  - *Проблема*: Сохранялась фраза «Сонечка, кажется, тебе придётся их рассудить» от старой игры «разделение потоков» (SplitGame), хотя день 5 — ремонт схемы питания колеса (`circuit`).
  - *Решение (Root Cause)*: Текст обновлён: Грутик и Веном объединяют усилия для ремонта искрящей цепи питания колеса.
- **Сюжет Дня 6 и объяснение поломки колеса (`GREET[5]` в `app.js`)**:
  - *Проблема*: На день 5 схему колеса чинили, а на день 6 колесо снова без объяснения ломалось (`wheelBroken`).
  - *Решение (Root Cause)*: Добавлено чёткое сюжетное обоснование: базового питания, починенного вчера, недостаточно для выдачи главного финального подарка. Механизм заблокирован защитой — «Протокол SONECHKA». Веном и Грутик объясняют, что нужно пройти трёхфазную синхронизацию всех систем для снятия блокировки. Кнопка дня: `Запустить протокол`.

#### 2. Кнопки выдачи подарков
- **Кнопки в сценах `AFTERGIFT[3]` и `DAY4_AFTER`**:
  - *Проблема*: В конце диалога дня 4 стояла кнопка `B("Забрать подарок")`, хотя модальное окно с подарком Сонечка уже открыла и изучила перед началом сцены.
  - *Решение (Root Cause)*: Кнопки приведены к стандарту всех остальных дней — `B("До завтра!")`.

#### 3. Логика стадий Грутика (`dayToStage`)
- **Стадия 8 до победы дня 6**:
  - *Проблема*: На 6-м дне (`d === 5`) функция `dayToStage` возвращала 8 ещё до прохождения финала, и стартовая сцена `GREET[5]` ошибочно содержала `S(8)`.
  - *Решение (Root Cause)*: На 6-м дне до победы строго возвращается стадия 7 (контролируемый симбиоз). Стадия 8 открывается исключительно после победы над финалом и вызова `unlockFinal()` (`state.finalForm = true`). Начало `GREET[5]` переведено на `S(7)`.
- **Режим Preview (`?preview=4..6`)**:
  - *Проблема*: При открытии `?preview=4`, `?preview=5`, `?preview=6` с чистым профилем флаг `state.venomInfected` был `false`, из-за чего функция `dayToStage` всегда отдавала стадию 4.
  - *Решение (Root Cause)*: В `dayToStage` и `lineForDay` добавлен явный учет параметра `window.__previewDay`:
    - preview=4 (d=3) $\to$ стадия 5 (Веном)
    - preview=5 (d=4) $\to$ стадия 7 (симбиоз)
    - preview=6 (d=5) $\to$ стадия 7 до победы (или 8 при `state.finalForm`)

#### 4. Block Blast: Разноцветные фигуры, Бесконечный режим, Кнопка рестарта
- **Разноцветные фигуры и клетки (`games.js`, `style.css`)**:
  - Добавлена палитра из 6 ярких сочных градиентов (Rose, Emerald, Amber, Violet, Azure, Gold).
  - При создании фигур в лотке (`newPieces`) каждой фигуре случайным образом назначается `color` (1..6).
  - Массив `this.board` хранит ID цвета установленной фигуры (1..6) вместо булевого флага.
  - Миниатюры в трее и drag-ghost окрашиваются в цвет фигуры.
  - Предпросмотр сброса (`previewBlockDrop`) подсвечивается оттенком перетаскиваемой фигуры.
  - Клетки поля получают классы `.block-color-1` ... `.block-color-6`.
  - Стартовые семена (`seedBoard`) также раскрашены по палитре.
- **Бесконечный режим на очки**:
  - После сбора 4 линий день считается пройденным, появляется поздравительный оверлей:
    - «К колесу 🎡» — мгновенный переход к сторилайну и колесу фортуны.
    - «Бесконечный режим 🏆» — продолжение игры на очки с сохранением лучшего результата в `localStorage` (`advent_blockblast_best`).
  - В бесконечном режиме ведётся постоянный подсчёт очков (за установку фигур и очистку линий).
  - При отсутствии ходов отображается экран Game Over с текущим счётом, рекордом, кнопками повторной игры и перехода к колесу.
  - В шапке игры в бесконечном режиме доступна кнопка перехода к колесу/выхода.
  - На экране ожидания (`screenWait`) после прохождения дня 2 доступна кнопка `🎮 Block Blast (бесконечный режим)`, позволяющая играть в любой момент.
- **Кнопка рестарта**:
  - В шапку игры добавлена кнопка `↺ Заново`.
  - Нажатие очищает поле, перегенерирует фигуры и обнуляет текущий счёт сессии.

#### 5. Тестирование
- Создан тестовый файл `tests/game_logic.test.js`.
- Добавлен `package.json` со скриптом `npm test`.
- Тесты проверяют:
  1. Корректность реплики дня 2 в `GRUTIK_LINES`.
  2. Соответствие текстов `GREET` актуальным играм дней 1, 4, 5, 6.
  3. Отсутствие кнопок «Забрать подарок» после выдачи призов.
  4. Маппинг стадий `dayToStage` в обычном и preview-режимах (стадия 8 только после финала).
  5. Генерацию и отображение цветных фигур Block Blast.
  6. Очистку линий, начисление очков и работу рестарта в Block Blast.
  7. Бесконечный режим и сохранение рекорда в `localStorage`.
  8. Реплики спутника в обычном и preview режимах.
- Все 8 тестов проходят успешно (`npm test` $\to$ 8 passed, 0 failed).

---

## Инструкция по запуску тестов
Для запуска тестов выполнить:
```bash
npm test
```
или напрямую:
```bash
node --test tests/game_logic.test.js
```

---

### 2026-09-13: Инициализация репозитория и публикация на GitHub

- **Git & Репозиторий**:
  - Создан файл `.gitignore` (исключение `node_modules/`, `.DS_Store`, `Thumbs.db`, `*.log`).
  - Проверены относительные пути к статическим ресурсам (`photos/`, стилям и скриптам) для совместимости с GitHub Pages.
  - Настроен удалённый репозиторий `origin`: `https://github.com/Shallow9902/AdventGame.git`.
  - Выполнен первый коммит и отправка ветки `main` в GitHub.

---

### 2026-09-14: Расширение сюжета и интерактивная система диалоговых выборов

#### 1. Механика ветвления сюжета (`C(key, prompt, options)`)
- **Движок сторилайна (`app.js`)**:
  - Добавлен конструктор шага `C(key, prompt, options)` в стори-очередь (`storyQueue`).
  - Каждый вариант ответа содержит `{ text, steps, cb }`.
  - При выборе ответа в начало очереди `storyQueue` подставляются персональные шаги выбранной ветки, что сохраняет неблокирующую очередь шагов.
  - Сохранение сделанного выбора в профиль игрока: `state.choices[key] = idx` в `localStorage`.
  - Очистка контейнера выборов при смене реплик, завершении сцены и старте новых историй.
- **Разметка и стили (`index.html`, `style.css`)**:
  - В `.story-card` добавлен контейнер `<div class="story-choices hidden" id="storyChoices"></div>`.
  - Стилизованы интерактивные кнопки `.story-choice-btn` с неоновыми фиолетово-розовыми градиентами, эффектом свечения, hover-анимацией и каскадным появлением (`scaleUp`, `fadeIn`).

#### 2. Расширение сюжетных линий и диалогов
- **Вступление (`INTRO`)**:
  - Интерактивный выбор: что делать с семечком (посадить сразу или сперва понаблюдать).
  - Реакция на первое появление Грутика: приветствие, погладить по листикам или удивление говорящему растению.
- **Приветствия дней (`GREET`)**:
  - **День 1**: Реакция на рассыпанные фотокарточки и растерянность Грутика («Не переживай, мы вместе найдём!» / «Покажи, что запомнил»).
  - **День 2**: Настрой на расчистку древесных блоков («Я разнесу тут всё!» / «Давай аккуратно, по плану»).
  - **День 3**: Появление странной тёмной субстанции («Грутик, не трогай!» / «Посмотреть поближе»).
  - **День 4**: Реакция на появление Венома и заражение Грутика («Грутик, с тобой всё хорошо?!» / «Эй, чёрное нечто, отпусти его!»).
  - **День 5**: Подход к ремонту искрящей живой цепи питания («Покажите мне схему» / «Просто подайте питание!»).
  - **День 6**: Настрой перед запуском «Протокола SONECHKA» («Готова! Запускаем!» / «Немного страшно...» с поддержкой от Грутика и Венома).
- **Победы и финалы (`POSTWIN`, `AFTERGIFT`, `FINAL_SCENE`)**:
  - **POSTWIN День 6**: В момент разделения симбиота Сонечка решает, останется ли Веном («Оставайся. Вы — команда» / «Грутик сам решит»).
  - **AFTERGIFT День 1**: Тёплое прощание на ночь («Спокойной ночи, Грутик!» / «До завтра, малыш!»).
  - **FINAL_SCENE**: Финальные слова Сонечки спутникам перед открытием письма («Спасибо, Грутик. Ты лучший» / «Обнимашки!» / «Я буду за тобой ухаживать»).
  - Дополнены диалоги и реакции в промежуточных днях (Веном поправляет рамку фото, переглядывания, уютные моменты).

#### 3. Тестирование
- Добавлен тест `Story Choice System` в `tests/game_logic.test.js` проверяющий конструктор `C()` и наличие выборов во всех сюжетных арках.
- Все 9 тестов успешно проходят (`npm test` $\to$ 9 passed, 0 failed).

---

### 2026-09-14: Каноничная речь Грута («Я есть Грутик») с субтитрами-переводом

#### 1. Механика речи и отображения
- **Конструктор реплик `L(who, text, trans)` (`app.js`)**:
  - Добавлен параметр `trans` для передачи перевода реплик персонажа.
  - Все реплики Грутика (`who: "gru"`) говорят строго каноничные фразы: `Я есть Грутик!`, `Я есть Грутик?`, `Я есть Грутик...` или `Я ЕСТЬ ГРУТИК!!!`.
  - Снизу под репликой автоматически отображается перевод в скобках: `(перевод)`.
  - В `renderLine` добавлен рендеринг двухслойной реплики: `<span class="groot-voice">` (крупный белый шрифт) и `<span class="groot-trans">` (аккуратный приглушённый курсив).
- **Облачко компаньона (`grutikBar` / `grutikSay`)**:
  - В `updateGrutikBar()` добавлена поддержка двухстрочных реплик с разделителем `\n`.
  - В `grutik.js` массив `GRUTIK_LINES` переведён в формат каноничной речи: `Я есть Грутик!` + `\n(перевод)`.
  - В `lineForDay()` реплики для preview и состояний заражения/симбиоза также оформлены с переводом.
- **Стили (`style.css`)**:
  - Стилизованы `.story-line.has-trans`, `.groot-voice` и `.groot-trans` для экрана сюжета и облачка диалога компаньона.

#### 2. Тестирование
- Тесты обновлены под каноничные реплики с переводом.
- Все 9 тестов проходят успешно (`npm test` $\to$ 9 passed, 0 failed).

---

### 2026-09-14: Привязка открытия дней к календарным датам (14–19 сентября)

#### 1. Логика календарного прогресса
- **Конфигурация (`gifts.js`)**:
  - Добавлен параметр `startDate: "2026-09-14"` в `SITE_CONFIG`.
- **Расчёт дня (`app.js`)**:
  - Добавлена функция `parseStartDate()`, преобразующая `SITE_CONFIG.startDate` в полночь стартового дня.
  - Функция `currentDayIdx()` теперь строго опирается на разницу календарных дней между сегодняшней датой и `SITE_CONFIG.startDate`:
    - 14 сентября $\to$ День 1 (индекс 0)
    - 15 сентября $\to$ День 2 (индекс 1)
    - 16 сентября $\to$ День 3 (индекс 2)
    - 17 сентября $\to$ День 4 (индекс 3)
    - 18 сентября $\to$ День 5 (индекс 4)
    - 19 сентября $\to$ День 6 (индекс 5)
  - Если игрок открывает игру до старта — гарантированно открывается День 1.
  - По прохождении Дня 1 Сонечка получает первый подарок, после чего видит таймер до полуночи следующего дня (15 сентября).

#### 2. Тестирование
- Добавлен тест `Calendar progression: 2026-09-14 is day 1, 2026-09-19 is day 6` в `tests/game_logic.test.js`.
- Все 10 тестов проходят успешно (`npm test` $\to$ 10 passed, 0 failed).

---

### 2026-09-14: Настройка пула подарков, привязка Веномизированного Грута к Дню 4 и доработка UI

#### 1. Упрощение оверлея игры «Память» (День 1)
- **Убран лишний текст (`games.js`)**:
  - Удалена громоздкая подсказка `"На поле спрятаны 12 пар ваших фотографий. Ошибаться можно сколько угодно; центральная фотография остаётся открытой."` в `MemoryPairsGame`.
  - В `GameBase.prototype.gate` добавлена проверка наличия текста: тег `<p class="gate-copy">` теперь рендерится только при наличии непустого описания.
  - На стартовом оверлее теперь отображается лишь лаконичный номер дня `01` и кнопка `Открыть воспоминания`.

#### 2. Реплики компаньона и финального экрана
- **Каноничная речь для всех состояний (`grutik.js`, `app.js`)**:
  - `GRUTIK_LINES[0]` (до посадки ростка) переведена в каноничный формат: `"Я есть Грутик...\n(Сигнал едва заметен. Внутри точно что-то есть.)"`.
  - `GRUTIK_LINES[4]` переведена в формат с субтитрами: `"Я есть Грутик!\n(...А МЫ — ВЕНОМ.)"`.
  - Реплики финального экрана (`FINAL_LINES` и `finalGrootSay`) также переведены на каноничный язык Грута с разделителем `\n` и субтитрами `<span class="groot-trans">`.

#### 3. Конфигурация подарков (`gifts.js`, `app.js`)
- **Список из 9 подарков (`GIFT_POOL` в `gifts.js`)**:
  1. `g1`: Адвент календарь для девочек Hello Kitty (`photos/1.webp`)
  2. `g2`: Игра для двоих | Али Хейзелвуд (`photos/2.webp`)
  3. `g3`: Любовь под омелой | Али Хейзелвуд, Тесса Бейли, Александрия Бельфлер, Алексис Дариа (`photos/3.webp`)
  4. `g4`: Принцесса Ардена | Софи Анри (`photos/4.webp`) — `active: false` (отключен)
  5. `g5`: Разреши любить. Позволь мне быть рядом | Джейн Анна (`photos/5.webp`)
  6. `g6`: Веномизированный Грут (`photos/6.webp`) — `specialDay: 3` (День 4)
  7. `g7`: LEGO Minifigures Человек-паук: Через вселенные (`photos/7.webp`) — `active: false` (отключен)
  8. `g8`: Фигурка "Как приручить дракона" (`photos/8.webp`) — `active: false` (отключен)
  9. `g9`: Носки Hello Kitty (`photos/9.webp`)
- **Механика колеса и привязка к сюжету (`app.js`)**:
  - `pool()` фильтрует подарки по флагу `active !== false` (неактивные 4, 7, 8 исключены из колеса).
  - `remainingPool(dayIdx)`: подарок `g6` («Веномизированный Грут») не доступен на Днях 1–3 (`d < 3`), исключая преждевременное выпадение.
  - В День 4 (`dayIdx === 3`), когда Грутик заражается симбиотом Веномом, `pickResult(pool, 3)` гарантированно выбирает `g6`.
  - В сцене `AFTERGIFT[3]` Грутик и Веном любуются выпавшей фигуркой Веномизированного Грута и собранной памятной фотографией.
  - В `grant()` проверка спец-сцены дня 4 обновлена с `g4` на `g6`.

#### 4. Тестирование
- В `tests/game_logic.test.js` добавлены тесты:
  1. `Gifts configuration: 9 items, inactive 4, 7, 8, and Day 4 Venomized Groot` (проверка пула, фильтрации и гарантированного выпадения `g6` на 4 день).
  2. `MemoryPairsGame gate overlay: no verbose description` (проверка чистоты стартового оверлея).
  3. `Companion lines: All GRUTIK_LINES follow canonical Groot speech with translation` (проверка формата всех реплик компаньона).
- Все 13 тестов успешно проходят (`npm test` $\to$ 13 passed, 0 failed).

---

### 2026-09-14: Засекречивание выпавших подарков (замыливание фото и отображение номера)

#### 1. Мотивация и Root Cause
- Чтобы сохранить интригу и сюрприз до момента физического вручения подарка в реальности, Сонечка не должна видеть название и открытое фото выбитого приза.
- При этом ведущему/автору подарка необходим номер подарка (1..9), чтобы точно знать, какую коробочку вручить.

#### 2. Реализация
- **Конфигурация (`gifts.js`)**:
  - В объекты `GIFT_POOL` добавлен явный числовой идентификатор `num: 1`..`9`.
- **Логика модального окна и истории (`app.js`)**:
  - В `normalizeGift` сохраняется `num`.
  - В `showGiftModal(gift, dayIdx)`:
    - Контейнер фотографии получает класс `.modal-photo.secret`.
    - Заголовок модального окна отображает: `Секретный подарок №${giftNum}`.
    - Текст подсказывает: `Содержимое засекречено, чтобы сохранить сюрприз!\nНазови номер №${giftNum}, чтобы забрать подарок в реальности 🎁`.
    - Поверх замыленного изображения создаётся стильный бейдж с иконкой замка, номером `№ ${giftNum}` и статусом `ЗАСЕКРЕЧЕНО`.
    - Прямая ссылка на товар скрыта.
  - В `grant()` в журнал `state.givenLog` сохраняются `num` и засекреченный заголовок.
  - В `renderFinal()` карточки итоговой сетки также отображаются в засекреченном виде (`final-item secret`) с бейджем номера `№ ${num}` и размытием.
- **Стилизация (`style.css`)**:
  - Для `.modal-photo.secret img` задан фильтр `filter: blur(18px) saturate(1.25) brightness(0.88)` с `transform: scale(1.14)` и запретом выделения, сохраняющий красивый цветовой силуэт без спойлера деталей.
  - Стилизованы оверлей `.gift-secret-badge`, неоновый номер `.gift-secret-number`, бейдж `.gift-secret-tag` и карточки `.final-item.secret img` (`blur(14px)`).

#### 3. Тестирование
- Добавлен тест `Secret gifts: won gifts are blurred and classified with gift numbers` в `tests/game_logic.test.js`.
- Все 14 тестов успешно проходят (`npm test` $\to$ 14 passed, 0 failed).